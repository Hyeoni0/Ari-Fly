# Ari Fly

A small touch-controlled web game inspired by Flappy-style obstacle dodging.

## Play

Open `index.html` directly, or serve the folder locally:

```sh
python3 -m http.server 5173
```

Then visit `http://localhost:5173`.

## Controls

- Touch / click: fly upward
- Space: fly upward
- R: restart
